import { serve, ServerRequest, Server } from "https://deno.land/std@0.53.0/http/server.ts";

// Centralize so we can control which version of server to import
export {
    serve,
    ServerRequest,
    Server,
};
